# 環境別のmodule設定は意図的な差分
module.vpc.environment
module.vpc.vpc_cidr

# 環境別のlocal変数は意図的な差分
local.common_tags
local.vpc_cidr

# 本番環境では追加のoutputが必要
output.vpc_cidr

# 本番環境では追加のvariableが必要
var.db_instance_class

# HCL関数は環境によって異なることが予想される
local.name_with_length
local.merged_tags
local.name_prefix
local.file_content