# 環境別のmodule設定は意図的な差分
module.vpc.environment
module.vpc.vpc_cidr

# 環境別のlocal変数は意図的な差分
local.common_tags
local.vpc_cidr

# 本番環境では追加のoutputが必要
output.vpc_cidr

# 本番環境では追加のvariableが必要
var.db_instance_class